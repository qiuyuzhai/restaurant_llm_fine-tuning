# 餐厅评论评分预测项目 - STAR描述

## Situation（情境）

在自然语言处理任务中，餐厅评论的情感分析和评分预测是一个重要的应用场景。传统的机器学习方法往往需要大量的特征工程，而大语言模型（LLM）的出现为这类任务提供了新的解决方案。本项目旨在系统性地评估和比较不同的大语言模型应用策略在餐厅评论评分预测任务上的表现，包括直接使用预训练模型的基线方法、基于少量示例的上下文学习方法，以及通过监督微调（SFT）进行模型适配的方法。

项目使用Qwen2.5-1.5B-Instruct作为基础模型，该模型是一个开源的指令调优大语言模型，具有良好的中文理解能力。数据集包含餐厅评论文本及其对应的1-5星评分标签，任务是将评论文本分类为对应的星级评分。

## Task（任务）

本项目的主要任务包括：

1. **Task A: Baseline Model（基线模型评估）**
   - 直接使用预训练的Qwen2.5-1.5B-Instruct模型进行零样本推理
   - 不提供任何训练示例，仅通过精心设计的提示词引导模型进行评分预测
   - 评估模型在未经过任务特定训练时的基础性能

2. **Task B: N-shot In-Context Learning（N-shot上下文学习）**
   - 使用5-shot学习策略，在提示词中提供5个代表性的训练示例
   - 通过智能选择中等长度（100-400字符）且具有代表性的评论作为示例
   - 评估少量示例对模型性能的提升效果

3. **Task C: Supervised Fine-Tuning（监督微调）**
   - 使用LoRA（Low-Rank Adaptation）技术对模型进行参数高效的微调
   - 将训练数据格式化为指令跟随格式，使用因果语言建模目标进行训练
   - 评估通过任务特定训练后模型的性能提升

所有任务都需要在相同的测试集上进行评估，使用准确率（Accuracy）、宏平均F1分数（F1-Score Macro）和加权平均F1分数（F1-Score Weighted）作为评估指标。

## Action（行动）

### 1. 系统架构设计

开发了一个统一的评估框架`RestaurantReviewPredictor`类，整合了三种不同的推理和训练方法，确保实验条件的一致性。

### 2. 数据处理

- 实现了自动编码检测功能，确保不同编码格式的CSV文件能够正确读取
- 对训练数据进行格式化处理，将评论和评分转换为指令跟随格式
- 使用HuggingFace的Dataset和Tokenizer进行数据预处理和分词

### 3. Baseline模型实现

- 设计结构化的提示词模板，明确说明评分标准和输出格式
- 使用温度采样（temperature=0.1）和top-p采样（top_p=0.8）进行文本生成
- 实现智能的评分提取算法，能够从模型输出中准确提取"X star"格式的评分

### 4. N-shot学习实现

- 开发了智能示例选择算法：
  - 从每个评分类别中选择代表性样本
  - 优先选择中等长度（100-400字符）的评论，避免过短（信息不足）或过长（上下文溢出）的示例
  - 选择最接近中位长度的样本作为最典型的代表
- 在提示词中动态插入选定的示例，形成few-shot学习提示

### 5. 监督微调实现

- 使用LoRA技术进行参数高效微调：
  - 设置rank=16，alpha=32，dropout=0.05
  - 针对注意力层和MLP层的投影矩阵进行适配
  - 仅训练约0.1%的模型参数，大幅降低计算和存储成本
- 训练配置：
  - 训练3个epoch
  - 批次大小4，梯度累积步数4（有效批次大小16）
  - 学习率2e-4，warmup步数100
  - 使用验证集进行模型选择

### 6. 评估和可视化

- 实现了统一的评估函数，计算准确率、宏平均F1和加权平均F1
- 生成详细的分类报告，展示每个类别的精确率、召回率和F1分数
- 保存预测错误案例用于分析
- 创建性能对比图表和表格，直观展示不同方法的性能差异

### 7. 实验流程

- 在相同的测试集上运行三种方法
- 确保所有方法使用相同的提示词结构和评分提取逻辑
- 记录详细的推理日志，便于后续分析

## Result（结果）

实验在餐厅评论测试集上取得了以下结果：

### 3.1 Task A: Baseline Model Results（基线模型结果）

基线模型（零样本推理）的性能指标如下：

| 评估指标 | 数值 |
|---------|------|
| Accuracy | 0.5511 |
| F1-Score (Macro) | 0.5118 |
| F1-Score (Weighted) | 0.5068 |

基线模型在未经过任务特定训练的情况下，达到了55.11%的准确率，表明预训练模型已经具备了一定的餐厅评论理解能力。

### 3.2 Task B: N-shot In-Context Learning Results（N-shot上下文学习结果）

使用5-shot学习策略的结果如下：

| 评估指标 | 数值 (n=5) | 相对基线模型的差异 |
|---------|-----------|------------------|
| Accuracy | 0.5511 | 0.0000 (+0.00%) |
| F1-Score (Macro) | 0.5180 | +0.0062 (+1.22%) |
| F1-Score (Weighted) | 0.5100 | +0.0040 (+0.88%) |

**分析**：5-shot学习在准确率上与基线模型相同，但在F1分数上有小幅提升（宏平均F1提升1.22%，加权平均F1提升0.88%）。这表明少量示例对模型性能有轻微的正向影响，但提升幅度有限。

### 3.3 Task C: SFT Fine-Tuned Model Results（监督微调模型结果）

使用LoRA进行监督微调后的结果如下：

| 评估指标 | 数值 | 相对基线模型的差异 |
|---------|------|------------------|
| Accuracy | 0.6475 | +0.0963 (+17.48%) |
| F1-Score (Macro) | 0.6444 | +0.1326 (+25.92%) |
| F1-Score (Weighted) | 0.6457 | +0.1398 (+27.62%) |

**分析**：监督微调模型在所有指标上都取得了显著的提升：
- **准确率提升17.48%**：从55.11%提升到64.75%
- **宏平均F1提升25.92%**：从51.18%提升到64.44%
- **加权平均F1提升27.62%**：从50.68%提升到64.57%

### 3.4 综合对比分析

1. **基线模型 vs N-shot学习**：
   - N-shot学习带来的提升非常有限（<2%），说明对于这个任务，仅通过少量示例难以显著改善模型性能
   - 可能的原因：任务相对复杂，需要更深入的任务理解，而不仅仅是模式匹配

2. **基线模型 vs 监督微调**：
   - 监督微调带来了**显著的性能提升**（17-28%），证明了任务特定训练的重要性
   - LoRA技术使得在保持计算效率的同时实现了性能的大幅提升

3. **方法选择建议**：
   - 对于快速原型和资源受限的场景，可以使用基线模型或N-shot学习
   - 对于追求最佳性能的生产环境，应该使用监督微调方法
   - LoRA技术提供了性能和效率之间的良好平衡

### 3.5 项目价值

本项目成功展示了：
- 大语言模型在餐厅评论评分预测任务上的应用潜力
- 不同应用策略（零样本、少样本、微调）的性能差异
- LoRA技术在参数高效微调中的有效性
- 统一的评估框架在方法对比中的重要性

最终，通过监督微调，模型在餐厅评论评分预测任务上达到了64.75%的准确率，相比基线模型提升了17.48%，证明了任务特定训练在提升模型性能方面的重要作用。
